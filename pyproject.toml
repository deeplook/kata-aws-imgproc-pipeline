[tool.uv]
dev-dependencies = [
    "boto3>=1.39.0",
    "opensearch-py>=2.4.0",
    "requests-aws4auth>=1.3.0",
    "pytest>=8.0.0",
    "moto[s3,rekognition,dynamodb]>=5.0.0",
    "ruff>=0.4.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
